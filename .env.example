# === SERVER CONFIGURATION ===
PORT=5000
NODE_ENV=development

# === DATABASE CONFIGURATION ===
# Coolify MongoDB Connection String
# Format: mongodb://username:password@host:port/database
# Example: mongodb://admin:password123@frozenshield-db:27017/frozenshield
MONGODB_URI=mongodb://YOUR_USERNAME:YOUR_PASSWORD@YOUR_MONGODB_HOST:27017/frozenshield

# === AUTHENTICATION ===
# JWT Secret for token signing (generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=GENERATE_A_LONG_RANDOM_STRING_HERE

# === EMAIL CONFIGURATION (OPTIONAL) ===
# For contact form notifications
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=hello@frozenshield.ca
# SMTP_PASS=your-gmail-app-password
# SMTP_FROM=hello@frozenshield.ca
